import { View } from 'react-native';

// Placeholder screen for the scan tab button.
// This never renders — the tabBarButton redirects to /scan.
export default function ScanPlaceholder() {
  return <View />;
}
