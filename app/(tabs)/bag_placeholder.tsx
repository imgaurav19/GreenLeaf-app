import { View } from 'react-native';

// Placeholder screen for the bag tab button.
// This never renders — the tabBarButton redirects to /checkout.
export default function BagPlaceholder() {
  return <View />;
}
